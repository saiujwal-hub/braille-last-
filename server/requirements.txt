# Braille Bridge server dependencies.
# CPU-first (hackathon friendly). Swap torch/torchvision for the CUDA build if you have a GPU.
#
# License notes:
#   opencv-python      Apache-2.0
#   torch/torchvision  BSD-3-Clause (via pip wheels)
#   ultralytics        AGPL-3.0 (PyPI-installed usage is fine)
#   realesrgan         BSD-3-Clause
#   basicsr            Apache-2.0
#   TTS (coqui)        MPL-2.0
#   fastapi/uvicorn    MIT
#
# Version pins are lower bounds; test with the newest patch releases.

# --- Core web ---
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.2

# --- Imaging ---
opencv-python>=4.9
numpy>=1.26
Pillow>=10.3

# --- Optional heavy ML ---
ultralytics>=8.2
torch>=2.2
torchvision>=0.17
# realesrgan>=0.3.0
# basicsr>=1.4.2

# --- Offline TTS (MPL-2.0) ---
# TTS>=0.22

# --- Optional SAM high-accuracy mode ---
# segment-anything @ git+https://github.com/facebookresearch/segment-anything.git

# --- Tests ---
pytest>=8.0
httpx>=0.27
